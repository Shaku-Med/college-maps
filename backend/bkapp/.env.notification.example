# Auto-copied to .env.notification with generated keys on first API start.
# If .env.notification already exists, the server will not replace it.
# Empty or invalid values leave notifications off; the rest of the API still runs.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
