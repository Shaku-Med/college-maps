# Optional. The API makes its own Web Push keys and keeps them in the database. Keys put here, in
# .env.notification, are only used to seed the database the first time, so subscriptions made with them
# keep working. The server never writes this file.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
