# Live reload for development: run `air` in this folder. Settings come from .env.development.
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/rt.exe ./cmd/rt"
entrypoint = ["./tmp/rt.exe"]
include_ext = ["go"]
exclude_dir = ["tmp", "vendor", "testdata"]
exclude_regex = ['_test\.go$']
delay = 500
stop_on_error = true

[misc]
clean_on_exit = true
